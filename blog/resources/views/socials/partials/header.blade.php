<section>
    <div class="flex justify-center mb-6">
        <h2 class="text-xl font-bold text-gray-800 dark:text-gray-200">
            @if(isset($socials->title))
                Edit Social
            @else
                Add Social
            @endif
        </h2>
    </div>
</section>